<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.model.dao.UserDAO">
	<insert id="insertUser" parameterType="User">
		insert into member
		values(#{id}, #{pw}, #{name}, #{address},#{phone}, #{zonecode}, #{detail_address})
	</insert>

	<select id="selectUser" parameterType="string" resultType="User">
		select * from member where
		id = #{id}
	</select>

	<delete id="deleteUser" parameterType="string">
		delete from member where id = #{id}
	</delete>
	
	<update id="updateUser" parameterType="User">
		update member set pw = #{pw}, name = #{name}, address = #{address}, phone = #{phone}, zonecode = #{zonecode}, detail_address = #{detail_address} where id = #{id}
	</update>
	
	<select id="idcheck" parameterType="string" resultType="int">
		select count(id)
		from member
		where id = #{id} 
	</select>

</mapper>