<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 


 <mapper namespace="com.example.model.dao.HouseInfoDAO">
 	<select id="searchHouseInfos" parameterType="string" resultType="HouseInfo">
 		select * from houseinfo where dongCode = #{dongCode}
 	</select>
 	
 	<select id="selectHouseInfo" parameterType="int" resultType="HouseInfo">
 		select * from houseinfo where aptCode = #{aptcode}
 	</select>
 	<select id="selectHouseInfoByName" parameterType="string" resultType="HouseInfo">
 		select * from houseinfo where aptName like concat('%',#{aptName},'%')
 	</select>
 	<select id="selectHouseInfoAll" resultType="HouseInfo">
 		select * from houseinfo
 	</select>
 	<select id="updateDistFromSubway" parameterType="HouseInfo">
 		update houseinfo set distFromSubway = #{distFromSubway} where aptCode = #{aptCode}
 	</select>
 	<select id="updateSubwayName" parameterType="HouseInfo">
 		update houseinfo set subwayName = #{subwayName} where aptCode = #{aptCode}
 	</select>
 	<select id="updateConvStoreCnt" parameterType="HouseInfo">
 		update houseinfo set convStoreCnt = #{convStoreCnt} where aptCode = #{aptCode}
 	</select>
 	<select id="updateRestaurantCnt" parameterType="HouseInfo">
 		update houseinfo set restaurantCnt = #{restaurantCnt} where aptCode = #{aptCode}
 	</select>
 	<select id="updateCafeCnt" parameterType="HouseInfo">
 		update houseinfo set cafeCnt = #{cafeCnt} where aptCode = #{aptCode}
 	</select>
 </mapper>